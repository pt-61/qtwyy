import QtQuick

Rectangle {
    color: leftColor

    Text {
        anchors.centerIn: parent
        text: "Logo"
        font {
            pixelSize: 24
            family: "黑体"
            bold: true
        }
    }
}
